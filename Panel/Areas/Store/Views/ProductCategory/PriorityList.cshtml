@model IEnumerable<DataLayer.Entities.ProductCategory>
@using DataLayer.Base
<div>
    <fieldset>
        <legend></legend>
        <div class="form-group">
            <div class="col-lg-12 font-bold">
                @foreach (var item in Model)
                {
                    if (item.Priority != null)
                    {
                        <a title="@item.GetName()"> @item.Priority.ToString() و</a>
                    }
                }
                ...
            </div>
        </div>
        <legend></legend>
    </fieldset>
</div>


