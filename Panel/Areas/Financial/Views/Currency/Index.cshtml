@model IEnumerable<DataLayer.Entities.Currency>

@{
    ViewBag.Title = "لیست واحد های پولی  ثبت شده در سایت";
    Layout = "~/Views/Shared/Layout.cshtml";
}
@section Footer {
    @Html.Action("TableScript", "Layout", new { area = "" })
}
@section Script {
    <script type="text/javascript">
        TableManageButtons.init();
    </script>
}
<div>
    <div class="col-sm-12">
        <div class="card-box table-responsive">
            <a href="@Url.Action("create")" class="icon circle-icon glyphicon glyphicon-plus" data-toggle="tooltip" data-placement="bottom" title="افزودن مورد جدید"></a>
            <table id="datatable-buttons" class="table table-striped table-bordered has-first">
                <thead>
                    <tr>
                        <th >نام واحد پول</th>
                        <th>علامت واحد پول</th>
                        <th>معادل  به ریال</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@Html.DisplayFor(modelItem => item.CurrencyName)</td>
                            <td>@Html.DisplayFor(modelItem => item.CurrencySign)</td>
                            <td>@Html.DisplayFor(modelItem => item.UnitPrice)</td>
                            <td>
                                @Html.ActionLink("ویرایش", "edit", new { id = item.CurrencyId }) |
                                @Html.ActionLink("جزئیات", "details", new { id = item.CurrencyId }) |
                                @Html.ActionLink("حذف", "delete", new { id = item.CurrencyId })
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
    <!-- end col -->
</div>
