@model IEnumerable<DataLayer.Entities.Post>
@using DataLayer.Base
@if (Model.Count() > 0)
{
    foreach (var item in Model)
    {
        <li>
            <div class="hvrArticle"></div><a href="@item.GetLinkWithUrl()">@Html.Raw(item.GetName())</a>
        </li>
    } 
}